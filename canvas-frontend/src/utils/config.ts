// Define these at the top of your file so they are consistent
export const DEFAULT_FONT_SIZE = 20; 
export const DEFAULT_FONT_FAMILY = "cursive"; // Or "Comic Sans MS", "Chalkboard SE"
export const DEFAULT_FONT_WEIGHT = "bold"; // Force bold on both to make them look same
export const BACKEND_URL = "http://127.0.0.1:3000"